{% extends "paragraph.html.twig" %}

{% block content %}
<div {{ attributes.addClass('wdn-band wdn-band-neutral-seperator wdn-light-neutral-band').addClass(classes) }}>
  <div class="wdn-inner-wrapper">
    <blockquote class="wdn-quote">
      {{ content.field_quote }}
      <cite class="wdn-quoter">
        {{ content.field_quoter }} <span class="quoter-context">{{ content.field_quoter_context }}</span>
      </cite>
    </blockquote>
  </div>
</div>
{% endblock %}
